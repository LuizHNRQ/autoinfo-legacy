import React from "react";
import { StyleSheet, View, Text } from "react-native";

const addUser = ({ navigation }) => {
  return (
    <View>
      <Text>Add usuario</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default addUser;
